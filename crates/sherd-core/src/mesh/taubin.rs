//! Taubin smoothing with Open3D's weights and iteration count (R §3.3): λ, μ and the neighbour
//! averaging exactly as `filter_smooth_taubin` does them, because the smoothed surface decides
//! the segmentation. Filled in by plan step S3.
