//! Edge adjacency and `closed_enough` (R §3.1, R §3.3): the edge→faces map that the segmentation
//! and the boundary count are built on, the boundary-edge fraction, and the watertightness test
//! that decides whether the penetration check runs for a fragment. Filled in by plan step S3.
