# V11 — independent verification of task R1: the rule, the table it was chosen on, and the gates

**Date:** 2026-09-11. **Tree:** branch `rust-core`, the ten commits `aefaaf8`…`6d1982a`
(`373520e..HEAD`), verified at `6d1982a`. **Machine:** Apple M2 Pro, 10 cores (6P + 4E), 16 GB,
macOS 24.6.0, rustc 1.97.0, `--release`, Python 3.12 in `.venv` (open3d 0.19.0, numpy 2.5.2,
scipy 1.18.1). Working tree at the start: clean, nothing to keep.

**Nothing below is taken from the note it checks.** Task R1's table was re-read here with the
rules written out again from `Thresholds::refusals` and `Thresholds::arm`, its AUC table with my
own rank statistic, its gate table both from the binary and offline from the dumps, and its
prediction for the one large run was computed before that run was scored. Where a figure of task
R1's is quoted it is quoted to be compared with mine. The scripts are under `output/v11/`.

PLACEHOLDER-VERDICT

---

## 1. What was checked

PLACEHOLDER-TABLE

---

PLACEHOLDER-BODY
